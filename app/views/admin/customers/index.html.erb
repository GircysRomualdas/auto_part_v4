<h1>Customers</h1>
<table class="table table-hover">
  <thead>
    <tr>
      <th>ID</th>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.id %></td>
        <td><%= customer.email %></td>
        <td><%= link_to "view", admin_customer_path(customer) %></td>
      </tr>
    <% end %>
  </tbody>
</table>