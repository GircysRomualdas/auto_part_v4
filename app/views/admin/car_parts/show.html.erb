<div>
  <h1>Show car part</h1>
  <div>
    <div>
      <%#= @car_part.image.present? ? image_tag(@car_part.image.variant(:show)) : image_tag("https://via.placeholder.com/800") %>
    </div>
    <p><strong>ID:</strong> <%= @car_part.id %></p>
    <p><strong>Name:</strong> <%= @car_part.name %></p>
    <p><strong>Description:</strong> <%= @car_part.description %></p>
    <p><strong>Price:</strong> <%= @car_part.price %> €</p>
    <p><strong>Active:</strong> <%= @car_part.active %></p>
    <p><strong>Type:</strong> <%= @car_part.part_type.name %></p>
  </div>
  <%= button_to "Delete", admin_car_part_path(@car_part), method: :delete, data: {turbo_confirm: "Are you sure?"} %>
</div>
